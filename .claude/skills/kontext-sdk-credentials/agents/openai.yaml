interface:
  display_name: "Kontext Server Credential Retrieval"
  short_description: "Retrieve a per-user or admin-managed integration credential for a confidential app"
  default_prompt: "Use $kontext-sdk-credentials to retrieve the credential available to a platform user from a confidential Kontext application."
